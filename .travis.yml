---
dist: trusty
sudo: required
group: edge
services: docker
language: python
python: 3.6

install: pip install ansible molecule docker-py
before_test:
  - cd ..
  - ln -s pacifica ansible-pacifica
  - cd pacifica
script: molecule test
